# ============================================================
# store-intelligence — Production Dependencies
# Python 3.11
# ============================================================

# --------------- Core API ---------------
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
sqlalchemy[asyncio]==2.0.30
aiosqlite==0.20.0          # SQLite async driver (swap to asyncpg for Postgres)
alembic==1.13.1

# --------------- Computer Vision pipeline ---------------
ultralytics==8.3.0         # YOLOv8 + ByteTrack built-in
opencv-python-headless==4.9.0.80
numpy==1.26.4
lapx>=0.5.2
Pillow==10.3.0
torch==2.5.1                # CPU-safe; GPU auto-detected
torchvision==0.20.1
# torchreid: install via git if PyPI release lags
# pip install git+https://github.com/KaiyangZhou/deep-person-reid.git
shapely==2.0.4              # Point-in-polygon for zone mapping

# --------------- Event processing ---------------
orjson==3.10.3              # Fast JSON serialisation
python-ulid==2.7.0          # Readable IDs (UUID v4 also accepted)

# --------------- Observability ---------------
structlog==24.1.0
python-json-logger==2.0.7

# --------------- Utilities ---------------
httpx==0.27.0               # Async HTTP client (also used in tests)
python-dotenv==1.0.1
click==8.1.7                # CLI entry point for pipeline/run.sh
rich==13.7.1                # Terminal dashboard

